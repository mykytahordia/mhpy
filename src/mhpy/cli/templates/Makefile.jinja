CONFIG_DIR ?= {{HYDRA_PATH}}
HYDRA ?=

.PHONY: train clean format check

check:
	uv run pre-commit run --all-files

train: 
	uv run python scripts/train.py --config-dir $(CONFIG_DIR) $(HYDRA)

clean:
	rm -rf __pycache__ .pytest_cache dist build *.egg-info

format:
	uv run ruff check --fix src